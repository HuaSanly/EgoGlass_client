# Interaction Processing Configuration

This directory will hold versioned phase, model, prompt, threshold, pose, and
visualization configuration. Defaults will be added with each implemented
stage rather than copied speculatively from HumanEgo.
