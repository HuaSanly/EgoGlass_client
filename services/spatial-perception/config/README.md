# Spatial Perception Configuration

This directory will hold versioned calibration selection, VIO backend, hand
tracking, time-association, and quality-threshold configuration. Defaults will
be added with the first implemented stage; the scaffold does not invent
hardware parameters.
